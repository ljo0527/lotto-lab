# 일확천금 · 다음 단계 제안

현재 상태에서 갈 수 있는 길들. **실제로 뭔가를 바꾸는 것부터** 순서대로 적었습니다.
완료된 항목은 ✅ 로 표시하고 그대로 남겨 둡니다 — 무엇을 왜 했는지의 기록이기도 합니다.

---

## 0. 지금 이미 확인된 것 — 이게 출발점

전 회차를 다 뒤진 결과, **번호 선택으로 바꿀 수 있는 건 딱 하나였습니다.**

| 시도 | 결과 |
|---|---|
| 잘 나오는 번호가 있나 | ❌ 카이제곱 검정 통과. 완전 균일. |
| 오래 안 나온 번호가 나올 때가 됐나 | ❌ 기하분포 그대로. 도박사의 오류. |
| 동반쌍에 패턴이 있나 | ❌ 상위 쌍도 이항분포 안. |
| 추첨 순서에 신호가 있나 | ❌ 순번별 카이제곱 전부 잡음. |
| **당첨자 수를 줄일 수 있나** | ✅ **가능. 독립된 두 모델의 예측 상관 0.895.** |

**핵심 논리** — 1·2·3등 당첨금은 고정 풀을 당첨자 수로 나눕니다.
확률은 못 바꿔도 **분모는 바꿀 수 있습니다.**

이걸 **서로 무관한 두 데이터**로 각각 쟀고, 답이 일치했습니다.

- **모델 A(판매 기준)** — 4·5등 당첨자 수 / 기대치. 4등 지수 × 5등 지수 상관 0.92
- **모델 B(구매방식 기준)** — 1등 중 수동 비율의 시대 편차. 262회~, 1등 9,174명 가중
- **두 모델 예측 상관 0.895.** 유의한 특징 일곱 개 전부 같은 방향

---

## 1. ✅ 완료 — 매주 5,000원(로또) + 5,000원(연금) 포트폴리오

2026-09 개편에서 「최소 분배」 전략을 실제 매주 구매 단위로 만들었습니다.

- **로또 A~E 5게임** — 인기도(위 모델) 최소화 + 5게임 간 겹침 ≤1개 커버리지 제약을 동시에 만족하는
  `portfolioPick` 알고리즘(`index.html`). 겹침 제약 덕에 5게임 중 하나라도 당첨될 주간 확률이 무작위
  5게임의 11.36%에서 11.75%로 올라갑니다 — **개별 확률이 아니라 묶음 커버리지**가 오른 것입니다.
- **연금 5장** — `pensionPortfolio`(`pension.html`)가 **분산**(끝자리 5개 모두 다름, 기본값)과 **세트**
  (같은 번호 × 5조) 두 구조를 계산합니다. 분산은 「1장이라도 당첨」 50%, 세트는 10% — 기대값은
  5,000원당 3,750원으로 **완전히 동일**합니다. 번호 자체가 아니라 5장이 어떻게 흩어지는지만 고르는
  선택입니다.
- 로또 1243회 / 연금 334회부터 적용. 그 이전 회차는 재현 시 당시 규칙(레거시) 그대로 나오도록
  라운드 게이팅을 넣어, 지난주 채점·원장이 계속 맞습니다.
- 실측치(회수율, R², z이득, pAny)는 매주 갱신되므로 이 문서에 숫자를 고정하지 않습니다 —
  **`validate.html`(검증)** 이 항상 그 시점의 실제 값을 보여줍니다.

## 2. ✅ 완료 — 자동/수동 모델 시간 분할 검증

당시 「1순위」로 남겨뒀던 항목입니다.

- **시간 분할(walk-forward) 검증 완료** — 941~1240회(이후 매주 확장창)로 걸어가며 재적합, out-of-sample
  R² 는 재보정 후 약 0.37 로 안정적으로 유지됩니다. `validate.html` 1층이 매주 이 값을 갱신합니다.
- **예측 로그 완료** — 매 회차 예측 인기도 vs 실제 4·5등 초과배수를 원장(`brief/validation.json`)에
  누적합니다.
- 2·3등의 수동 비율 확장 경로는 여전히 미발견(공식 API가 1등만 제공) — 아래 §5로 이월합니다.

## 3. ✅ 완료(자동화) — 알림/갱신을 예약 작업 없이

과거엔 PC의 Cowork 예약 작업(금요일 연금, 일요일 로또)이 데이터 갱신과 재계산을 맡았습니다.
이제 **GitHub Actions**(`.github/workflows/weekly.yml`)가 push 또는 스케줄로 전부 계산·커밋·배포합니다.
PC를 켜 둘 필요가 없어졌고, `setup/triggers/*.md`의 예약 작업은 읽기 전용 용도가 아니면 꺼 두는 것을
권장합니다(자세한 내용은 `setup/triggers/README.md`).

- 남은 것: 4등 이상일 때만 알림처럼 「조용하지 않은」 알림 채널(현재는 브리핑 페이지를 직접 보는 방식).
  `remind.ics`(정적 캘린더 알림)까지는 있고, 능동 푸시는 아직 없습니다.

## 4. ✅ 완료 — 전략 리그전 (검증 보드)

`validate.mjs`가 매주 `portfolio`/`portfolio_carry`/`disjoint`/`weekly_v1`(구 규칙) + `minshare`/`maxshare`/
`random`/`hot`/`cold`를 **동일한 회차·동일한 후보 풀**로 나란히 채점합니다. 확률은 전략과 무관하게
동일해야 하고(대조군으로 확인됨), 차이는 1등 당첨 시 나눠 갖는 인원에서만 나야 한다는 예측을
매주 재검증합니다. `validate.html` 2층이 이 표입니다.

---

## 5. 다음 단계 — 2026-09 개편 이후

### 5.1 carry(전주 회피) 게이트 전환

전주 당첨번호와 겹치는 번호를 피하면(공식 데이터 상 사람들이 실제로 덜 사는 경향) 4·5등 초과배수가
평균 약 1.3%/개 더 올라간다는 신호가 있습니다(walk-forward γ=−0.0127, t=−4.4, OOS R² 0.368→0.394).
지금은 `PORTFOLIO.carry=false`로 꺼 둔 채 `portfolio_carry`를 **그림자 전략**으로만 검증 보드에 같이
채점하고 있습니다. 게이트 조건(walk-forward t < −3 이고 ΔR² > 0)이 계속 통과하면, 다음은:

- 몇 주 더 그림자 성적을 쌓아 게이트가 우연이 아닌지 재확인
- 통과가 안정되면 `PORTFOLIO.carry=true`로 전환하고, 이번 주 커버리지(P(any))가 소폭 낮아진다는
  트레이드오프(1243회 기준 11.75% → 약 11.51%)를 브리핑에 명시
- 켜기 전까지는 계속 그림자로만 두고, 실제 구매 규칙은 바꾸지 않습니다

### 5.2 등수별(tier) 기울기 분리

지금은 4·5등에서 측정한 보정 기울기(b≈0.033)를 1~3등에도 그대로 적용합니다 — 표본이 작은 상위 등수의
잡음을 줄이기 위해 **의도적으로 보수적인** 선택입니다. 그런데 등수별로 따로 회귀하면 기울기가
0.085(1등)·0.060(2등)·0.051(3등)·0.033(4/5등)으로 등수가 높을수록(희귀할수록) 더 가파릅니다
(t값 3.3/2.2/6.7/13.8). 즉 지금 방식은 상위 등수의 효과를 **과소평가**하고 있을 가능성이 있습니다.

- 등수별 기울기가 회차를 더 쌓아도 유의하게 유지되는지 확인 (현재 1·2등은 λ≈15/89로 Poisson 잡음이
  커서 회차당 OOS R² 가 사실상 0에 가까움 — 표본이 훨씬 더 쌓여야 판별 가능)
- 유의하면 등수별 기울기를 EV 계산(`lineEV`)에 반영하고, 검증 보드에 「등수별 보정」 표를 추가
- 유의하지 않으면(현재 상태) 지금의 보수적 공통 기울기를 계속 씁니다 — 이 항목은 판별 전까지 «참고용
  표시»로만 검증 보드에 남깁니다

### 5.3 그 외 미탐색 항목 (기존)

- **분배 모델 고도화** — 번호별 개별 인기도(45개 더미), 생일 패턴, 마크시트 기하학, 직전 회차 재사용
  개수(§5.1의 carry와 일부 중복), 연도별 시간 추세
- **미탐색 조합 공간** — 8,145,060개 중 1,238개만 실제로 나왔습니다. 하위 인기도 조합을 미리 계산해
  캐시해 두는 것도 방법
- **실물 연결** — 마크시트 PDF 정합, QR 스캔 당첨확인, 1등 판매점 지도
- **추첨순서 데이터 보강** — 397~467회 공백 복원 시도(통계 결론에는 영향 없을 가능성이 높음)

---

## 마지막으로

이 프로젝트로 돈을 벌 수는 없습니다. 로또의 기대 회수율은 구조적으로 약 50%, 연금복권720+는
5,000원당 기대값 3,750원으로 고정입니다.

바꿀 수 있는 건 로또는 **"터졌을 때 얼마를 나눠 받느냐"**, 연금복권은 **"5장이 어떻게 흩어지느냐"**
뿐입니다. 구체적인 크기는 계산할 때마다 갱신되므로 여기 고정 숫자를 적지 않습니다 —
**최신 실측 효과 크기는 항상 [`validate.html`](./validate.html)(검증) 을 보세요.**
(참고로 예전에 여기 적혀 있던 «±10~15%»는 재보정 전 계수로 계산한 값이라 과대평가였고, 지금은 쓰지 않습니다.)

그 정도를 위해 이만큼 판 것이고, 그건 그것대로 충분히 재밌는 일입니다.

---

작성: 이재오
